import React, {Component} from 'react';
import './app.less';
class Timer extends Component {
  render() {
	return (
		<div className="timer">
		  this is a timer
		</div>
	);
  }
}

export default Timer;
